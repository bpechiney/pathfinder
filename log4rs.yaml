appenders:
  console:
    kind: console
    encoder:
      pattern: "{h({l})} {d(%H:%M:%S%.6f)} [{T}] {m}{n}"
    filters:
      - kind: threshold
        level: info

root:
  level: info
  appenders:
    - console